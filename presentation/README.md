# Präsentation (Abgabe)

Lege hier die **Quelldatei** der Folien (z. B. PowerPoint `.pptx`, LaTeX Beamer, Markdown → Marp) und das **exportierte PDF** ab, so wie es die Hochschule verlangt.

**Demo für den Vortrag:** Im Projektroot nach aktivierter venv:

```text
python -m stateful_signatures
```

Kurz einordnen: Lamport-OTS als vereinfachtes Modell; XMSS in den Folien/Theorie.

Siehe auch `docs/PHASE_D_INTEGRATION.md`, Abschnitt 5 (Folien-Anker).
